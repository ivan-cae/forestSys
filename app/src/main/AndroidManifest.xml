<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.forestsys">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permisson.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".ApplicationTodos"
        android:allowBackup="true"
        android:icon="@mipmap/icone_gelf"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.FragmentoInsumos"></activity>
        <activity android:name=".activities.ActivityListagemApontamentos" />
        <activity android:name=".activities.ActivityListagemCalibracao" />
        <activity android:name=".activities.FragmentoApontamento" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.AActivityTesteRelogio"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ActivityMain"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ActivityConfiguracoes"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ActivityCalibracao"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ActivityApontamentos"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ActivityAtividades"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ActivityDashboard"
            android:screenOrientation="portrait" />
        <activity
            android:name=".calculadora.i.CalculadoraMain"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ActivityInicializacao"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ActivityLogin"
            android:label="@string/apk_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>