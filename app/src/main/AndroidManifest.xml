<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.forestsys">

    <uses-permission android:name="android.permisson.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConfiguracoesActivity"></activity>
        <activity android:name=".ValidarAdminActivity" />
        <activity android:name=".CadastrarUsuarioActivity" />
        <activity android:name=".CalibragemActivity" />
        <activity android:name=".IniciarColetaActivity" />
        <activity android:name=".ContinuarOSActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/apk_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>